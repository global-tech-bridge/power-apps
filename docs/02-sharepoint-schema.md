# SharePoint データ設計

> このファイルは `data/list-schema.json` から自動生成される。
> 直接編集せず、JSON を直して `python3 scripts/gen-schema-doc.py` を実行すること。

要件定義 11章に対応する。`scripts/Provision-SharePoint.ps1` が同じ JSON を読んで
リスト・列・インデックスを作成するため、ここに書かれている内容がそのまま構築される。

## 列名を英語にしている理由

SharePoint は列を日本語名で作ると、内部名が `_x9867__x5ba2__x540d_` のような
エスケープ文字列になる。Power Automate の OData フィルター、Word テンプレートの
差し込み、REST 呼び出しはすべて内部名を使うため、日本語名にすると式が読めなくなり
保守できない。そのため列は英語名で作り、画面上の日本語ラベルは Power Apps 側の
ラベルコントロールで与えている（要件定義 11.1 の列名定義にも合わせている）。

## 画面から作る場合

PnP.PowerShell が使えない環境では、下の表のとおりに画面で列を作る。
手順は [08 画面だけで構築する手順](08-manual-setup.md) を参照。

- **列名は必ず表のまま（英語）で入力する。** 日本語で作ると内部名が壊れる
- 「索引」が ○ の列は、リストの設定 → **インデックスされた列** で追加する
  （委任可能な絞り込みと件数増加のため。要件定義 15.2）
- 列の作成時に「既定のビューに追加する」を外すと、一覧が見やすくなる

## リスト

### SignatureCases

整備キャンセル料 同意書の署名案件（要件定義 11.1）

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `DocumentNo` | 1行テキスト | — | ○ | — | 文書番号 YYYYMMDD-NNN。採番はフローが行う |
| `CustomerName` | 1行テキスト | ○ | ○ | — | 顧客名 |
| `CustomerEmail` | 1行テキスト | — | — | — | 署名済みPDFの送付先 |
| `CustomerPhone` | 1行テキスト | — | — | — | 顧客電話番号（任意） |
| `Model` | 1行テキスト | — | — | — | 型式 |
| `SerialNo` | 1行テキスト | — | — | — | 機番 |
| `NoSerialNo` | はい/いいえ | — | — | 既定値: **いいえ** | 機番なしの機体（要件定義 19章-3 の暫定対応） |
| `MaintenanceType` | 選択肢<br>`点検整備` / `一般整備` / `その他` | — | — | 選択肢に左記の値を1行ずつ入力／値を手動で追加できる: **いいえ**／複数選択: **いいえ** | 整備区分 |
| `Comment` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** | コメント／顧客ご用命事項 |
| `BranchName` | 1行テキスト | — | ○ | — | 支社（組織マスタの部門名。docs/07-open-issues.md D-01） |
| `BlockName` | 1行テキスト | — | ○ | — | ブロック |
| `BlockCode` | 1行テキスト | — | — | — | ブロックコード |
| `SiteName` | 1行テキスト | — | ○ | — | 拠点 |
| `SiteCode` | 1行テキスト | — | ○ | — | 拠点コード（変換前拠点コード） |
| `Status` | 選択肢<br>`Draft` / `Signed` / `PdfCreated` / `Stored` / `Sent` / `Error` | — | ○ | 選択肢に左記の値を1行ずつ入力／値を手動で追加できる: **いいえ**／複数選択: **いいえ** | 内部コードで保持し、画面の日本語ラベルはアプリ側の colStatus で対応付ける |
| `ConsentId` | 1行テキスト | — | — | — | 同意文面ID |
| `ConsentTitle` | 1行テキスト | — | — | — | 同意文面の表示タイトル（PDFの見出しに使う） |
| `ConsentVersion` | 1行テキスト | — | — | — | 同意文面の版 |
| `ConsentTextSnapshot` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** | 署名時点の文面全文。マスタ改訂後も署名時の内容を再現するため |
| `SignedAt` | 日付と時刻 | — | ○ | 含める内容: **日付と時刻** | 署名日時 |
| `SignerName` | 1行テキスト | — | — | — | 署名者お名前（任意入力） |
| `Operator` | ユーザーまたはグループ | — | — | 複数選択: **いいえ**／選択の対象: **ユーザーのみ** | ログイン利用者 |
| `OperatorEmail` | 1行テキスト | — | ○ | — | 委任可能な絞り込み用に文字列でも保持する |
| `OperatorName` | 1行テキスト | — | ○ | — | 担当者表示名 |
| `SignatureImageUrl` | 1行テキスト | — | — | — | SignatureImages ライブラリ内の署名PNGのURL |
| `PdfUrl` | 1行テキスト | — | — | — | SignatureDocs ライブラリ内の署名済みPDFのURL |
| `PdfFileName` | 1行テキスト | — | — | — | PDFファイル名（文書番号.pdf） |
| `RegisteredAt` | 日付と時刻 | — | ○ | 含める内容: **日付と時刻** | 作成日。Created 列はサイトの言語で表示名が変わるため独自に持つ |
| `SentAt` | 日付と時刻 | — | — | 含める内容: **日付と時刻** | メール送信完了日時 |
| `ResendCount` | 数値 | — | — | 小数点以下の桁数: **0**／既定値: **0** | 再送回数 |
| `ClientRequestId` | 1行テキスト | — | ○ | — | 冪等キー。再実行で二重採番・二重PDFを防ぐ |
| `ErrorCode` | 1行テキスト | — | — | — | エラー識別子 |
| `ErrorMessage` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** | エラー内容 |
| `FlowRunId` | 1行テキスト | — | — | — | フロー実行ID（調査用） |

### OrgMaster

組織マスタ（支社→ブロック→拠点。部門マスタ.xlsx 由来／要件定義 11.2）

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `BranchName` | 1行テキスト | — | ○ | — |  |
| `BlockCode` | 1行テキスト | — | — | — |  |
| `BlockName` | 1行テキスト | — | ○ | — |  |
| `SiteCode` | 1行テキスト | — | ○ | — |  |
| `SiteName` | 1行テキスト | — | — | — |  |
| `IsActive` | はい/いいえ | — | — | 既定値: **はい** |  |
| `SortOrder` | 数値 | — | — | 小数点以下の桁数: **0** |  |

初期データ: `data/OrgMaster.csv`

### ConsentMaster

同意文面マスタ（要件定義 11.3）

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `ConsentId` | 1行テキスト | — | ○ | — |  |
| `Version` | 1行テキスト | — | — | — |  |
| `EffectiveFrom` | 日付と時刻 | — | ○ | 含める内容: **日付と時刻** |  |
| `EffectiveTo` | 日付と時刻 | — | — | 含める内容: **日付と時刻** |  |
| `IsActive` | はい/いいえ | — | — | 既定値: **はい** |  |
| `DisplayTitle` | 1行テキスト | — | — | — |  |
| `Body` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** |  |
| `RevisedBy` | 1行テキスト | — | — | — |  |
| `RevisedAt` | 日付と時刻 | — | — | 含める内容: **日付と時刻** |  |

### AppAdmins

アプリ管理者（削除権限の判定に使う／要件定義 5.2）

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `UserEmail` | 1行テキスト | — | ○ | — |  |
| `UserName` | 1行テキスト | — | — | — |  |
| `IsActive` | はい/いいえ | — | — | 既定値: **はい** |  |
| `Note` | 1行テキスト | — | — | — |  |

### SendLog

メール送信履歴（初回送信・再送／要件定義 13章）

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `CaseId` | 数値 | — | ○ | 小数点以下の桁数: **0** |  |
| `DocumentNo` | 1行テキスト | — | ○ | — |  |
| `SentTo` | 1行テキスト | — | — | — |  |
| `SentBy` | 1行テキスト | — | — | — |  |
| `SentAt` | 日付と時刻 | — | ○ | 含める内容: **日付と時刻** |  |
| `Kind` | 選択肢<br>`Initial` / `Resend` | — | — | 選択肢に左記の値を1行ずつ入力／値を手動で追加できる: **いいえ**／複数選択: **いいえ** |  |
| `Result` | 選択肢<br>`Success` / `Failure` | — | — | 選択肢に左記の値を1行ずつ入力／値を手動で追加できる: **いいえ**／複数選択: **いいえ** |  |
| `ErrorMessage` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** |  |

### AuditLog

監査ログ（削除・再送・同意文面変更／要件定義 15.4）

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `Action` | 選択肢<br>`Delete` / `Resend` / `ConsentChange` / `Other` | — | — | 選択肢に左記の値を1行ずつ入力／値を手動で追加できる: **いいえ**／複数選択: **いいえ** |  |
| `CaseId` | 数値 | — | ○ | 小数点以下の桁数: **0** |  |
| `DocumentNo` | 1行テキスト | — | ○ | — |  |
| `PerformedBy` | 1行テキスト | — | ○ | — |  |
| `PerformedAt` | 日付と時刻 | — | ○ | 含める内容: **日付と時刻** |  |
| `Reason` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** |  |
| `Detail` | 複数行テキスト | — | — | リッチ テキスト: **いいえ**（プレーン テキスト）／変更内容を追加: **いいえ** |  |

### DocumentNumberCounter

文書番号の日次連番カウンタ（Title = yyyyMMdd／要件定義 8章）。フローの同時実行数を1に固定して直列化する。

| 列名 | 画面で選ぶ種類 | 必須 | 索引 | 画面での追加設定 | 内容 |
|---|---|:-:|:-:|---|---|
| `Title` | 1行テキスト | ○ | — | 既定である列。作成不要 | SharePoint の既定必須列 |
| `LastNumber` | 数値 | — | — | 小数点以下の桁数: **0**／既定値: **0** |  |

## ドキュメント ライブラリ

| 名前 | 用途 |
|---|---|
| `SignatureDocs` | 署名済みPDFの正本。通常利用者は読み取りのみ（要件定義 10章 / 17.2） |
| `SignatureImages` | 手書き署名のPNG |
| `DocTemplates` | PDF生成に使う Word テンプレート（templates/整備キャンセル料同意書.docx） |
| `WorkTemp` | Word→PDF 変換の中間ファイル置き場。個人 OneDrive を中継点にしないため（要件定義 15.5）。フローが処理後に削除する |

### ファイル名の付け方

| ライブラリ | ファイル名 |
|---|---|
| `SignatureDocs` | `<文書番号>.pdf` — 例 `20260909-001.pdf` |
| `SignatureImages` | `<文書番号>.png` |
| `WorkTemp` | `<文書番号>.docx`（変換後にフローが削除する） |

顧客名をファイル名に含めないのは、顧客へ送るメールやPDFのプロパティに
余分な情報を残さないため（要件定義 15.3）。また、文書番号だけで一意に決まるので、
初回送信でも再実行でもフローが同じパスでファイルを取得できる。

## ステータスの遷移

```
Draft ──署名送信──> Signed ──PDF作成──> Stored ──メール送信──> Sent
  │                    │                  │                    │
  └────────────────────┴──────────────────┴────────────────────┘
                    いずれかで失敗すると Error
                    （エラー画面から再実行すると失敗箇所から再開する）
```

`PdfCreated` は要件定義 9章で定義されているが、本実装では PDF の作成と
SharePoint への保存を1つのフロー内で連続して行うため、`Stored` に集約している。
選択肢としては残してあるので、将来 PDF 作成と保存を分離する場合に使える。

